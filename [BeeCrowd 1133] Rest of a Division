#include <stdio.h>

int main()
{
    int X, Y;
    scanf("%d %d", &X, &Y);

    int min = (X < Y) ? X : Y;
    int max = (X > Y) ? X : Y;

    for (int i = min + 1; i < max; i++)
    {
        if (i % 5 == 2 || i % 5 == 3)
        {
            printf("%d\n", i);
        }
    }
    return 0;
}
